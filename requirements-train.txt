# Training dependencies - run on the cloud T4 (Kaggle/Colab), NOT on Hamza's CPU box.
# Unsloth pins compatible torch/transformers/trl/peft via its install extras, so on
# Kaggle prefer the one-liner in the notebook:
#   pip install "unsloth[kaggle-new] @ git+https://github.com/unslothai/unsloth.git"
# This file documents the headless (scripts/train.py) install for a generic CUDA box.

unsloth>=2026.6.1
unsloth_zoo>=2026.6.1
torch>=2.4.0
transformers>=4.46.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
datasets>=3.0.0
matplotlib>=3.8.0          # loss.png
huggingface_hub>=0.26.0
